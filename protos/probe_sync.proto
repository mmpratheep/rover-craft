syntax = "proto3";
package probe_sync;

service ProbeSync {
  rpc ReadProbe (ReadProbeRequest) returns (ProbeData);
  rpc WriteProbe (ProbeData) returns (WriteProbeResponse);
}

message ReadProbeRequest {
  string probe_id = 1;
}

message ProbeData {
  string probe_id = 1;
  string event_id = 2;
  uint64 event_date_time = 3 ;
  string data = 4;
}

message WriteProbeResponse {
  bool confirmation = 1;
}
